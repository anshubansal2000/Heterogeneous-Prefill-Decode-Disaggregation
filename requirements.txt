# pdbench harness (client side — runs anywhere)
httpx>=0.27
aiohttp>=3.9        # disagg proxy (pdbench/proxy.py)
matplotlib>=3.7
numpy
transformers>=4.44        # vocab lookup for token-id prompts

# server side (installed on the RunPod GPU pod by scripts/setup_runpod.sh):
#   vllm>=0.10            # provides `vllm serve` + the OpenAI server + /metrics
#   torch (CUDA build)    # pulled in by vllm
